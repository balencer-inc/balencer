-- =====================================================================
-- キャンペーンにフリーコメント欄を追加
-- AIへの追加指示を自然言語で渡す（業種・エリア・規模では表現できないニュアンス）
-- =====================================================================

alter table campaigns
  add column if not exists additional_instructions text;
