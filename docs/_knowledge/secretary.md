# secretary の知見ログ

> 各担当が作業で得た知見・決定・好み・命名規則・却下案とその理由を蓄積する。
> 1エントリは `## YYYY-MM-DD 要点` の形。運用ルールは README.md を参照。

<!-- ここに追記していく -->

## 2026-07-03 業務改善アーキテクト回: 工程スキル3本と自動化台帳を新設

- **決定**: 担当（役割）スキルとは別に「工程スキル」という層を新設。担当をまたぐ共通ツールとして `brand-lint`（対外文書の自動検品）・`estimate`（見積3点セット半自動）・`weekly-review`（週次定点観測）の3本を `.claude/skills/` に追加した。
- **決定**: 対外成果物は報告前に `python3 scripts/brand_lint.py`（SNSは `--sns`）を必ず通す。ERRORは修正必須、WARNは阿部さんに見せて判断を仰ぐ。社内文書（_knowledge等）はNGワードを列挙しているため検品対象外。
- **決定**: 社内用スクリプトの置き場所は `scripts/` に統一（CLAUDE.md 3章に追記済み）。
- **決定**: 自動化候補はその都度思いつきで着手せず、台帳 `docs/_knowledge/ai-automation-roadmap.md` に追記してから優先度を付ける。業務マップ・17件の候補・3分類（Quick Win / 業務OS / 高難度）を記載済み。
- **却下案**: pre-commitフックでのbrand-lint強制（候補#16）は見送り。社内文書の誤検知で作業が止まるリスクがあるため、まず手動運用で誤検知率を見る。
- **保留（要承認）**: SNS下書きのデイリー自動生成（候補#5）はクラウドルーティンの課金が発生するため、阿部さんの承認を得てから設定する。
